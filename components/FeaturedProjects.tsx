import Link from "next/link";
import { projects, type Project } from "@/data/projects";
function ProjectLinks({ project }: { project: Project }) { const entries = [{ label: "Live Demo", url: project.links.demo }, { label: "GitHub", url: project.links.github }]; return <div className="mt-7 flex flex-wrap gap-2"><Link href={project.route} className="button-primary !min-h-9 !px-3 !py-1 text-xs">{project.slug === "category-learning" ? "View Research" : "Read Case Study"} ↗</Link>{entries.map(({ label, url }) => url ? <a key={label} href={url} target="_blank" rel="noreferrer" className="button-secondary !min-h-9 !px-3 !py-1 text-xs">{label} ↗</a> : <span key={label} className="inline-flex items-center rounded-md border border-slate-200 px-3 py-2 text-xs font-semibold text-slate-400" title={`${label} link coming soon`}>{label} · Soon</span>)}</div>; }
function ProjectCard({ project, featured = false }: { project: Project; featured?: boolean }) { return <article className={`surface overflow-hidden rounded-xl ${featured ? "grid lg:grid-cols-[1.05fr_.95fr]" : "flex flex-col"}`}><div className={`${featured ? "p-7 sm:p-10 lg:p-12" : "p-7 sm:p-8"}`}><div className="mb-7 flex items-center justify-between gap-3"><span className="eyebrow">{project.type}</span><span className="font-mono text-xs text-slate-400">/{project.number}</span></div><h3 className={`${featured ? "max-w-lg text-3xl sm:text-4xl" : "text-2xl"} font-bold leading-tight tracking-[-.04em]`}>{project.title}</h3><p className="mt-5 max-w-xl leading-7 text-slate-600">{project.description}</p>{project.slug === "category-learning" && <p className="mt-4 border-l-2 border-violet-300 pl-3 text-sm leading-6 text-slate-500">Comparing clean labels and entrenched label noise in human and neural-network learning. Research is ongoing.</p>}<div className="mt-7 flex flex-wrap gap-2">{project.technologies.map(tech => <span className="tag" key={tech}>{tech}</span>)}</div><ProjectLinks project={project}/></div>{featured && <div className="flex min-h-72 items-center justify-center bg-[#eaf0ff] p-6 sm:p-10"><div className="w-full max-w-sm rounded-xl border border-blue-100 bg-white p-5 shadow-xl shadow-blue-900/10"><div className="flex items-center gap-2 border-b border-slate-100 pb-4"><span className="size-2 rounded-full bg-blue-500"/><span className="text-xs font-bold text-slate-600">Research workspace</span><span className="ml-auto text-[10px] text-slate-400">DOCUMENTS</span></div><div className="mt-5 space-y-3"><div className="rounded-md border border-slate-200 bg-slate-50 p-3"><span className="text-xs font-semibold text-slate-700">Uploaded literature</span><div className="mt-2 h-1.5 w-3/4 rounded bg-blue-100"/></div><div className="rounded-md border border-blue-100 bg-blue-50 p-3"><span className="text-xs font-semibold text-blue-800">Question grounded in sources</span><div className="mt-2 h-1.5 w-5/6 rounded bg-blue-200"/><div className="mt-2 h-1.5 w-2/3 rounded bg-blue-100"/></div><div className="flex items-center gap-2 rounded-md bg-violet-50 p-3 text-xs font-semibold text-violet-700"><span className="grid size-5 place-items-center rounded bg-violet-100">↗</span> Source citations</div></div></div></div>}</article>; }
export default function FeaturedProjects() { return <section id="projects" className="py-18 sm:py-24"><div className="container-wide"><div className="mb-10 flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="eyebrow mb-4">Selected work</p><h2 className="section-heading">Featured Projects<span className="text-blue-600">.</span></h2></div><p className="max-w-sm text-sm leading-6 text-slate-500">Software, applied AI, and research built around real questions and useful outcomes.</p></div><div className="space-y-5"><ProjectCard project={projects[0]} featured/><div className="grid gap-5 md:grid-cols-2">{projects.slice(1).map(project => <ProjectCard key={project.slug} project={project}/>)}</div></div></div></section>; }
