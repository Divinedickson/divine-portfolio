// UI-only assistant drawer reserved for a later phase.
export type ChatDrawerProps = { open: boolean; onClose: () => void; sendMessage?: (message: string) => Promise<void> };
export default function ChatDrawer({ open, onClose }: ChatDrawerProps) { if (!open) return null; return <aside role="dialog" aria-modal="true" aria-labelledby="chat-title" className="fixed inset-0 z-50 bg-white p-6 sm:inset-y-0 sm:left-auto sm:w-[420px] sm:shadow-2xl"><div className="flex items-start justify-between"><div><h2 id="chat-title" className="text-xl font-bold">Ask about Divine</h2><p className="text-sm text-slate-500">Portfolio AI Assistant</p></div><button onClick={onClose} aria-label="Close assistant" className="rounded-md p-2 hover:bg-slate-100">✕</button></div><p className="mt-10 rounded-lg bg-blue-50 p-4 text-sm leading-6 text-slate-700">Hi! I can answer questions about Divine&apos;s projects, technical skills, experience, and research.</p><p className="mt-8 text-sm text-slate-500">The assistant interface is coming in a later phase.</p></aside>; }
